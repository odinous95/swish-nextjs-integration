import React from "react";

export default async function CheckoutStatusPage() {
    // we will make a function that make call tot the api to check the status

    return (
        <main className="checkout-status-page" style={{ padding: "2rem", textAlign: "center" }}>
            <h1>Checkout Status</h1>
            {/* <p>{getStatusMessage()}</p> */}
        </main>
    );
};
